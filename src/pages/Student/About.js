import React from 'react'
import StudentLayout from '../../layouts/StudentLayout'

const About = (props) => {

  return (
    <StudentLayout match={props.match}>
      
    </StudentLayout>
  )
}

export default About
